(t => { const e = document.body, n = document.getElementById("total"); let o = 0; const l = t => Math.floor(Math.random() * t); e.addEventListener("click", () => { o += 300, n.textContent = `$${o}`, function () { let t = e.clientHeight, n = e.clientWidth, o = document.createElement("span"); o.textContent = "+$300", o.style.top = `${l(t)}px`, o.style.left = `${l(n)}px`, e.insertAdjacentElement("afterbegin", o), setTimeout(() => { e.removeChild(o) }, 700) }() }) })();